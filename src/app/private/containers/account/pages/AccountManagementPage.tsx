import StatusIcon, { STATUS } from '../../../../../components/icon/StatusIcon';

const AccountManagementPage: React.FC = () => {
  return <StatusIcon status={STATUS.UNDER_CONSTRUCTION} />;
};

export default AccountManagementPage;
